[package]
name = "ffi"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib", "staticlib"]
name = "meeting_assistant_ffi"

[dependencies]
uniffi = { version = "0.28", features = ["tokio"] }

[build-dependencies]
uniffi = { version = "0.28", features = ["build"] }
