# CLAUDE.md

Claude Code must read and follow the shared repository instructions in [RULES.md](RULES.md).

`RULES.md` is the single source of truth for baseline agent behavior in this repository. Keep `AGENTS.md` and `CLAUDE.md` aligned so Codex and Claude Code receive the same project rules.
